BOOTSTRAP: localimage
FROM: bougui_1.sif

%files

%post

# Install aider-chat without dependencies first to avoid setuptools compatibility issues
pip install --no-deps --upgrade --upgrade-strategy only-if-needed 'aider-chat[browser]'

# Manually install required dependencies
pip install --upgrade --upgrade-strategy only-if-needed setuptools wheel

# %runscript
# cmd $@
